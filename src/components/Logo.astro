---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`flex items-center gap-2 ${className}`}>
  <svg
    viewBox="0 0 32 32"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    class="w-8 h-8 md:w-8 md:h-8 w-10 h-10"
  >
    <path
      d="M16 4L28 24H4L16 4Z"
      fill="currentColor"
      class="text-primary"
    />
  </svg>
  <span class="hidden md:inline font-extrabold text-xl tracking-tighter text-foreground">
    PARA<span class="text-primary">ANALİZ</span>
  </span>
</div>
